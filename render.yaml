services:
  - type: web
    name: german-class-tool
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: Dockerfile
    envVars:
      - key: FLASK_ENV
        value: production
      - key: VITE_ENV
        value: production
      - key: VITE_API_BASE_URL
        value: ""  # Empty means use relative URLs (required!)
  - type: web
    name: backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn --chdir backend/src --bind 0.0.0.0:5050 app:app
    envVars:
      - key: REDIS_HOST
        value: redis
    dependsOn:
      - redis
  - type: redis
    name: redis
    plan: free
